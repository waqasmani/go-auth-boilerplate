package auth

// mapper.go is intentionally minimal — auth responses are built directly
// in service.go via TokenResponse. This file is reserved for future
// mapping helpers (e.g., converting db.User → UserSummary).
